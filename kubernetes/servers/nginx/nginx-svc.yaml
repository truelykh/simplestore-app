apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: nginx
  labels:
    app.kubernetes.io/name: nginx
spec:
  type: NodePort
  selector:
    app: nginx-pod
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080

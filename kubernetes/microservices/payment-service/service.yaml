apiVersion: v1
kind: Service
metadata:
  name: payment-svc
  namespace: storeapp
  labels:
    app.kubernetes.io/name: payment-service
    app.kubernetes.io/part-of: storeapp
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080

apiVersion: v1
kind: Service
metadata:
  name: product-svc
  namespace: storeapp
  labels:
    app.kubernetes.io/name: product-service
    app.kubernetes.io/part-of: storeapp
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080

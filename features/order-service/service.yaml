apiVersion: v1
kind: Service
metadata:
  name: order-svc
  namespace: storeapp
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: storeapp
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080

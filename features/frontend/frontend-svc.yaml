apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: storeapp
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80

# notification-service

Spring Boot demo service.

Endpoints:

```text
GET  /api/notifications
POST /api/notifications
GET  /actuator/health
```

Build:

```bash
mvn -B -Drevision=1.0 clean package
```
